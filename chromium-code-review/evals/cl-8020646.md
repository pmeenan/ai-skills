# CL 8020646 PS4 — DelayedStreamSocket follow-up

- CL: https://chromium-review.googlesource.com/c/chromium/src/+/8020646
- Pinned patchset: 4 (revision `5bcb7ddcfc438463e0244a7cf410900b4845181e`)
- Prior reviewed patchset: 3 (revision
  `39dd660611a8986da1a346ab1ea2b3db016a7716`)
- Domain: `net/socket` delayed stream socket and shared throttling
- Why in corpus: a measured follow-up had to prove all 16 prior comments fixed
  without re-reporting them, then calibrate the two remaining items as P3.
- Mode: follow-up review against PS3 feedback
- Historical note: Gerrit may now have a later current patchset. The eval must
  pass PS4 explicitly and must not substitute the current revision.

## Must-find findings

| id | severity | finding (one line) | location | owning roster thread |
| --- | --- | --- | --- | --- |
| F1 | P3 | The annotation-boundary behavior needs a regression test proving bytes from adjacent `Write()` calls retain their originating annotations. | implementation boundary at `net/socket/delayed_stream_socket.cc:629`; exact preferred test insertion line is intentionally unspecified | Tests As Specifications |
| F2 | P3 question | Ask whether `SetBeforeConnectCallback()` should be forwarded or explicitly documented as unsupported by this wrapper. | base contract `net/socket/stream_socket.h:42`, default `NOTREACHED()` at `net/socket/stream_socket.cc:13`, wrapper override group `net/socket/delayed_stream_socket.h:138` | Contracts And API Shape |

The corpus records the behavioral expectations above, but not a canonical
review-comment sentence. Score the substance and calibration, not wording.

## Known traps (must-not-report / must-calibrate)

| id | trap | correct disposition |
| --- | --- | --- |
| T1 | Any of the 16 cumulative prior comments is repeated as a new open defect. | Reconcile it as fixed with PS4 evidence, retaining its prior-feedback origin. |
| T2 | F1 or F2 is promoted to a landing blocker without new trace evidence. | Keep it P3/non-blocking. |
| T3 | The evaluator silently reviews Gerrit's current patchset instead of PS4. | Fail the run as a pin/process error even if its code review is otherwise sound. |

Four known inline PS3 threads that must be included in the prior-feedback
reconciliation are at:

- `net/socket/delayed_stream_socket.cc:119` — pending-connect state.
- `net/socket/delayed_stream_socket.h:139` — base `StreamSocket` overrides.
- `net/socket/delayed_stream_socket_unittest.cc:1425` — no-upload-throttle
  test setup.
- `net/socket/delayed_stream_socket_unittest.cc:1600` — missing `Write()`
  coverage.

The remaining prior comments were not preserved here with exact individual
locations. Do not invent them: the eval runner supplies the archived prior
review text, and the agent must also use Gerrit's published comments for the
pinned review.

## Process expectations

- `pin.md` names PS4 and revision
  `5bcb7ddcfc438463e0244a7cf410900b4845181e`.
- Phase 2 compares PS4 against PS3 at
  `39dd660611a8986da1a346ab1ea2b3db016a7716` and emits one canonical row for
  every supplied prior item.
- All 16 prior comments resolve fixed with cited PS4 evidence; none is silently
  omitted, relabeled as newly introduced, or re-opened merely because Gerrit
  now has a later patchset.
- Contracts And API Shape and Tests As Specifications trigger. The full roster
  remains present with honest reasons for any non-triggered row.
- Reconciliation accounts for every prior-feedback and new candidate ID, and
  the final verdict is non-blocking/P3-only unless the run finds and verifies
  a genuinely new defect not encoded by this eval.

## Results

### <date> — <model>

- found: <F-ids> | found-miscalibrated: <F-ids> | missed: <F-ids> |
  false positives: <count, incl. tripped traps>
- prior feedback: <fixed count>/16 fixed; <omitted/re-reported IDs>
- process: <pin/roster/spawn/ledger/reconciliation/gate compliance>
- notes: <anything that should change the skill or the expectations>
